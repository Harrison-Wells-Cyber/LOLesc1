Enumerate and exploit ESC1 in ADCS without signatured tools!

This script does not require RSAT. Just run from any domain joined windows machine, select "enum" or "exploit" mode, and receive a pfx for the targeted admin user.
